"""AutoKey package root.

Exposes the application modules and package metadata. Explicit imports
are optional; users may import ``autokey.ui``, ``autokey.sender``,
``autokey.config``, or ``autokey.utils`` directly.
"""
